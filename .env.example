# Required at build time (Vite inlines VITE_* vars into the bundle).
# Set these in the Netlify dashboard for the deployed site; copy this file
# to .env.local for local dev (and never commit .env.local).

VITE_SUPABASE_URL=https://mvbporfpjjpnnjgjobdx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
